# Runtime dependencies for the existing notebook workflow.
# These are constrained ranges, not a fully locked environment.

numpy>=1.24,<3.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
pyarrow>=12,<18
jupyterlab>=4.0,<5.0
